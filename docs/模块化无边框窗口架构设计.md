# Rivet 模块化无边框窗口架构

> **适用项目**: RealChuan/Rivet  
> **技术栈**: Electron 42 + React 19 + Vite 8 + Tailwind CSS 4 + TypeScript 6  

---

## 1. 架构总览

### 1.1 核心设计

```
┌──────────────────────────────────────────────────────────────┐
│                      主进程 (Main Process)                    │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              WindowFactory.create(options)            │   │
│  │  ┌────────────┐ ┌────────────┐ ┌──────────────────┐  │   │
│  │  │ MainWindow │ │SettingsWin │ │  PreviewWindow   │  │   │
│  │  │  route: /  │ │ route: /s  │ │   route: /prev   │  │   │
│  │  │  id: main  │ │  id: set   │ │    id: preview   │  │   │
│  │  └────────────┘ └────────────┘ └──────────────────┘  │   │
│  │         ↑ 统一注入 frameless 配置 + 通用 Preload      │   │
│  └──────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
                                │
┌──────────────────────────────────────────────────────────────┐
│                    渲染进程 (Renderer Process)                │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              <TitleBar /> 通用组件                     │   │
│  │         (自动识别平台 / 支持 childMode)                │   │
│  │  ┌────────────┐ ┌────────────┐ ┌──────────────────┐  │   │
│  │  │ Main Page  │ │SettingsPage│ │  Preview Page    │  │   │
│  │  │+ TitleBar  │ │+ TitleBar  │ │  + TitleBar      │  │   │
│  │  └────────────┘ └────────────┘ └──────────────────┘  │   │
│  └──────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

### 1.2 核心文件清单

| # | 文件路径 | 说明 |
|---|---------|------|
| 1 | `src/main/core/window-factory.ts` | 无边框窗口工厂 + WindowManager |
| 2 | `src/main/core/main.ts` | 应用入口，注册全局 IPC |
| 3 | `src/main/preload/index.ts` | 通用 Preload，暴露 windowControl API |
| 4 | `src/renderer/components/ui/TitleBar.tsx` | 跨平台通用标题栏组件 |
| 5 | `src/renderer/styles/index.css` | Tailwind 工具类（.draggable / .no-drag） |

---

## 2. 主进程：窗口工厂模块

### 2.1 文件：`src/main/core/window-factory.ts`

**核心功能**：统一创建无边框窗口，自动处理平台差异。

#### 2.1.1 类型定义

```typescript
export interface FramelessWindowOptions {
  id: string                    // 窗口唯一标识
  route?: string                // 渲染进程路由路径
  title?: string               // 窗口标题
  width?: number               // 宽度（默认 1200）
  height?: number              // 高度（默认 800）
  minWidth?: number            // 最小宽度（默认 800）
  minHeight?: number           // 最小高度（默认 600）
  parent?: BrowserWindow       // 父窗口（模态对话框）
  modal?: boolean              // 是否模态
  alwaysOnTop?: boolean        // 是否置顶
  resizable?: boolean          // 是否可调整大小
  show?: boolean               // 是否立即显示
}
```

#### 2.1.2 平台差异处理

| 平台 | frame | titleBarStyle | 说明 |
|------|-------|---------------|------|
| macOS | `true` | `'hidden'` | 保留系统交通灯按钮 |
| Windows | `false` | `'default'` | 完全由 React 接管 |
| Linux | `false` | `'default'` | 完全由 React 接管 |

#### 2.1.3 窗口状态事件转发

```typescript
win.on('maximize', () => {
  win.webContents.send('window-state-changed', {
    windowId: options.id,
    isMaximized: true,
  })
})

win.on('unmaximize', () => {
  win.webContents.send('window-state-changed', {
    windowId: options.id,
    isMaximized: false,
  })
})
```

#### 2.1.4 WindowManager API

| 方法 | 说明 |
|------|------|
| `create(options)` | 创建并注册窗口（防止重复） |
| `get(id)` | 通过 ID 获取窗口 |
| `close(id)` | 通过 ID 关闭窗口 |
| `getAll()` | 获取所有存活窗口 |
| `broadcast(channel, ...args)` | 向所有窗口广播消息 |

---

## 3. 主进程入口

### 3.1 文件：`src/main/core/main.ts`

**职责**：注册窗口控制 IPC 处理器，创建主窗口。

#### 3.1.1 窗口控制 IPC

```typescript
ipcMain.on('window-minimize', event => {
  const win = BrowserWindow.fromWebContents(event.sender)
  win?.minimize()
})

ipcMain.on('window-maximize', event => {
  const win = BrowserWindow.fromWebContents(event.sender)
  if (win?.isMaximized()) {
    win.unmaximize()
  } else {
    win?.maximize()
  }
})

ipcMain.on('window-close', event => {
  const win = BrowserWindow.fromWebContents(event.sender)
  win?.close()
})

ipcMain.handle('window-get-state', event => {
  const win = BrowserWindow.fromWebContents(event.sender)
  return {
    isMaximized: win?.isMaximized() ?? false,
    platform: process.platform,
  }
})
```

#### 3.1.2 子窗口管理 IPC

```typescript
ipcMain.handle('window-create', (_event, options) => {
  WindowManager.create({
    ...options,
    width: options.width ?? 800,
    height: options.height ?? 600,
  })
  return options.id
})

ipcMain.handle('window-close-by-id', (_event, id) => {
  WindowManager.close(id)
  return true
})
```

---

## 4. Preload 脚本

### 4.1 文件：`src/main/preload/index.ts`

**安全原则**：仅通过 `contextBridge` 暴露白名单 API。

#### 4.1.1 windowControl API

```typescript
const windowControl = {
  minimize: () => ipcRenderer.send('window-minimize'),
  maximize: () => ipcRenderer.send('window-maximize'),
  close: () => ipcRenderer.send('window-close'),
  getState: () => ipcRenderer.invoke('window-get-state'),
  onStateChange: (callback) => {
    const handler = (_: IpcRendererEvent, state) => callback(state)
    ipcRenderer.on('window-state-changed', handler)
    return () => ipcRenderer.removeListener('window-state-changed', handler)
  },
  createChild: (options) => ipcRenderer.invoke('window-create', options),
  closeChild: (id) => ipcRenderer.invoke('window-close-by-id', id),
}
```

#### 4.1.2 窗口元数据

```typescript
function getWindowMeta() {
  const args = process.argv
  const idArg = args.find(a => a.startsWith('--window-id='))
  const routeArg = args.find(a => a.startsWith('--route='))
  return {
    windowId: idArg?.replace('--window-id=', '') ?? 'unknown',
    route: routeArg?.replace('--route=', '') ?? '/',
  }
}
```

#### 4.1.3 暴露到全局

```typescript
contextBridge.exposeInMainWorld('electronAPI', {
  windowControl,
  windowMeta: getWindowMeta(),
  protocol: protocolAPI,
  common: commonAPI,
})
```

---

## 5. 标题栏组件

### 5.1 文件：`src/renderer/components/ui/TitleBar.tsx`

**特性**：

- 自动识别平台（macOS / Windows / Linux）
- 支持 `childMode`：子窗口隐藏最小化/最大化按钮
- 支持自定义中间内容和左侧内容
- 零外部图标依赖（内联 SVG）

#### 5.1.1 Props 定义

```typescript
export interface TitleBarProps {
  childMode?: boolean          // 子窗口模式
  centerContent?: React.ReactNode  // 中间区域自定义内容
  leftContent?: React.ReactNode    // 左侧额外内容
  title?: string               // 自定义标题（默认 'Rivet'）
}
```

#### 5.1.2 macOS 标题栏

```tsx
if (isMac) {
  return (
    <header className="h-9 bg-bg border-b border-border flex items-center justify-center draggable titlebar-text shrink-0">
      <span className="text-xs text-text-muted font-medium tracking-wide">{title}</span>
    </header>
  )
}
```

#### 5.1.3 Windows/Linux 标题栏

```tsx
<header className="h-9 bg-bg border-b border-border flex items-center justify-between shrink-0">
  {/* 左侧：拖拽区 + Logo + 标题 */}
  <div className="flex-1 flex items-center h-full px-3 draggable titlebar-text"
       onDoubleClick={childMode ? undefined : handleMaximize}>
    <div className="flex items-center gap-2 no-drag">
      <AppLogo />
      <span className="text-sm text-text font-semibold">{title}</span>
      {leftContent}
    </div>
  </div>

  {/* 右侧：控制按钮 */}
  <div className="flex h-full items-center gap-1 px-2 no-drag">
    {centerContent}
    {!childMode && (
      <>
        <button onClick={handleMinimize}><IconMinus /></button>
        <button onClick={handleMaximize}>{isMaximized ? <IconMaximize2 /> : <IconSquare />}</button>
      </>
    )}
    <button onClick={handleClose}><IconX /></button>
  </div>
</header>
```

---

## 6. Tailwind 工具类

### 6.1 文件：`src/renderer/styles/index.css`

```css
@layer utilities {
  /**
   * 使元素成为窗口拖拽区域
   */
  .draggable {
    -webkit-app-region: drag;
    app-region: drag;
  }

  /**
   * 在拖拽区域内排除特定元素（可正常接收鼠标事件）
   */
  .no-drag {
    -webkit-app-region: no-drag;
    app-region: no-drag;
  }

  /**
   * 标题栏文字防选中
   */
  .titlebar-text {
    user-select: none;
    -webkit-user-select: none;
  }
}
```

---

## 7. 平台差异速查表

| 特性 | macOS | Windows | Linux |
|------|-------|---------|-------|
| 主进程配置 | `frame: true` + `titleBarStyle: 'hidden'` | `frame: false` | `frame: false` |
| 系统交通灯 | ✅ 自动渲染 | ❌ 无 | ❌ 无 |
| 自定义按钮 | 不需要 | React 实现 | React 实现 |
| 窗口拖拽 | `.draggable` 类 | `.draggable` 类 | `.draggable` 类 |
| 双击最大化 | 系统默认支持 | 需手动绑定 `onDoubleClick` | 需手动绑定 `onDoubleClick` |

---

## 8. 使用示例

### 8.1 创建主窗口

```typescript
// main.ts
WindowManager.create({
  id: 'main',
  route: '/',
  width: 1000,
  height: 700,
  title: 'Rivet',
})
```

### 8.2 创建子窗口

```typescript
// 渲染进程中
await window.electronAPI.windowControl.createChild({
  id: 'settings',
  route: '/settings',
  width: 700,
  height: 500,
  title: 'Settings — Rivet',
})
```

### 8.3 使用 TitleBar 组件

```tsx
import { TitleBar } from '@renderer/components/ui/TitleBar'

function App() {
  return (
    <div className="h-screen flex flex-col">
      <TitleBar
        title="Rivet"
        centerContent={<div>自定义内容</div>}
        leftContent={<div>左侧内容</div>}
      />
      <main className="flex-1">
        {/* 页面内容 */}
      </main>
    </div>
  )
}
```

---

## 9. 验证检查清单

- [ ] **主窗口**：无边框，可拖拽，按钮工作正常
- [ ] **macOS**：交通灯按钮可见且位置正确
- [ ] **Windows/Linux**：自定义按钮样式正确，hover 效果正常
- [ ] **子窗口**：可通过 `createChild` 打开，关闭按钮工作
- [ ] **重复打开**：同一 `id` 不会重复创建，而是聚焦已有窗口
- [ ] **双击最大化**：Windows/Linux 标题栏左侧双击可最大化/还原
- [ ] **主题切换**：标题栏正确响应深色/浅色模式

---

## 10. FAQ

### Q1: 标题栏按钮点击无反应？

**排查**：

1. 按钮元素是否有 `no-drag` class？
2. Preload 是否正确暴露 `windowControl`？
3. 主进程 IPC 处理器是否已注册？

### Q2: 子窗口的 `id` 重复会怎样？

WindowManager 会检测到已存在的窗口并执行 `focus()`，不会重复创建。

### Q3: 开发时 Vite HMR 导致 `window.electronAPI` 丢失？

Electron 的 Preload 只在窗口加载时注入一次。刷新整个窗口（Ctrl+R / Cmd+R）即可恢复。

---

*最后更新: 2026-05-10*  
*适用 Electron 版本: ^42.0.1*
